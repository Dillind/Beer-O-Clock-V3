import { Container, Typography } from "@mui/material";
const Home = () => {
  return (
    <Container fixed>
      <Typography variant="h4">Home</Typography>
    </Container>
  );
};

export default Home;
